# Shared model facts injected into BOTH engines at deploy time (single source of truth).
# deploy.sh sources this and injects the values so the vLLM values and the SGLang manifest
# cannot drift. Only these four (+ native context for YaRN) are shared; engine-specific tuning
# (quantization, speculative decoding, KV dtype) lives in each engine's own config.
MODEL_ID=Qwen/Qwen3.8-27B
SERVED_MODEL_NAME=Qwen/Qwen3.8-27B
NATIVE_CONTEXT=262144
YARN_FACTOR=4
# The deployed window and its matching concurrency come from common/context-profiles.env,
# selected with `deploy.sh --context 131k|262k|1m`. They are one decision (a longer window
# means fewer sequences fit), so they are not set here where only one of the pair would be.
# NATIVE_CONTEXT and YARN_FACTOR stay: they are facts about the model, and deploy.sh applies
# YaRN only when the chosen window exceeds native.
