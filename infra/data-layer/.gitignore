# Terraform working files — never commit state (contains real resource ids/ARNs) or the
# downloaded provider plugins. State belongs in the S3 remote backend (see versions.tf).
.terraform/
*.tfstate
*.tfstate.*
crash.log
crash.*.log
*.tfvars.local
override.tf
override.tf.json
*_override.tf
*_override.tf.json
