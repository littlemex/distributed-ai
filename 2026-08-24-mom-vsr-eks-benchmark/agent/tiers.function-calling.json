{
  "_comment": [
    "The tier definitions for the function-calling arm (docs/PROTOCOL.md).",
    "",
    "gpt-5.6-terra speaks the Responses API here, at reasoning_effort 'high'. That is not a",
    "preference: this gateway's /v1/chat/completions refuses function tools together with any",
    "reasoning_effort other than 'none', and the text arm ran this tier at 'high' with 63.3% of its",
    "output tokens being reasoning. Sending it to /openai/v1/responses is what keeps the arm",
    "comparing protocols rather than comparing a reasoning model against itself with reasoning off.",
    "",
    "(The chat-completions refusal names '/v1/responses'. This gateway serves it at",
    "'/openai/v1/responses'; the bare path is a 404.)",
    "",
    "claude-fable-5 needs no change: it accepts function tools on chat completions as configured."
  ],
  "premium": {
    "model": "claude-fable-5",
    "pricing_key": "fable",
    "reasoning_effort": "high"
  },
  "cheap": {
    "model": "gpt-5.6-terra",
    "pricing_key": "gpt-5.6-terra",
    "reasoning_effort": "high",
    "api": "responses"
  },
  "self_hosted": {
    "model": "Qwen/Qwen3.6-35B-A3B",
    "api_key_env": null,
    "rate": {
      "fresh_in": 0.236,
      "out": 4.12,
      "cache_read": 0.0188,
      "cache_write": 0.236
    },
    "rate_basis": "measured 2026-08-29 on Qwen3.6-35B-A3B-FP8, g6e.12xlarge at $15.2174/h, TP=2 x 2 replicas. fresh_in from 18,417 prefill tok/s aggregate at 16 in flight; cache_read from 224,620 tok/s of cached prefill measured the same way, which is 8.2% of a fresh token (benchctl/cached_prefill_rate.py). cache_write priced as fresh input because the engine does the same work to populate a block as to compute it. THIS REPLACES the earlier $10.68 flat rate, whose basis said prefix caching is unsupported on the hybrid attention layers -- that was a missing opt-in flag rather than an architectural limit, so the old rate priced every repeated prompt as recomputed and overstated the box input rate by about 45x."
  }
}
